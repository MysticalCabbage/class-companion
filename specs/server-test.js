/*var request = require('supertest');
var express = require('express');
var app = require('../server/server.js');

var req = request(app);

describe('Should respond from a GET to /', function() {
  it('Should respond with status code 200', function(done) {
    request(app)
      .get('/')
      .expect(200)
      .end(done);
  });

  it('Should respond with content-type html', function(done) {
    request(app)
      .get('/')
      .expect('Content-Type', /html/)
      .end(done);
  });
});*/
